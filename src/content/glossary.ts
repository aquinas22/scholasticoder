export interface GlossaryEntry {
  term: string
  definition: string
  /** Learning path slugs where the idea is taught. */
  paths?: string[]
  aliases?: string[]
}

/** Plain-language definitions of the words that trip people up. Kept alphabetical. */
export const glossary: GlossaryEntry[] = [
  { term: 'Algorithm', definition: 'A precise sequence of steps that turns an input into an output. Sorting a list, finding a route, and compressing an image are all algorithms; the same one can be written in any language.', paths: ['dsa', 'python'] },
  { term: 'API', definition: 'Application Programming Interface: the set of functions, endpoints or commands one piece of software exposes so another can use it. A web API is usually URLs that accept and return JSON.', paths: ['javascript', 'python', 'internet'], aliases: ['REST', 'endpoint'] },
  { term: 'Argument', definition: 'A value you pass into a function when calling it. The names in the function definition are parameters; the values at the call site are arguments.', paths: ['python', 'javascript'], aliases: ['parameter'] },
  { term: 'Array', definition: 'An ordered collection of values accessed by numeric index, starting at 0. Python calls its flexible version a list; C arrays are fixed-size blocks of memory.', paths: ['javascript', 'c', 'dsa'], aliases: ['list'] },
  { term: 'Assembly', definition: 'Human-readable names for a CPU\'s raw instructions. One line of assembly is one machine operation, which is why it is the last stop before the hardware.', paths: ['asm', 'computer-architecture'] },
  { term: 'Async / await', definition: 'Syntax for writing code that waits on slow operations (network, disk) without blocking everything else. await pauses one task; other tasks keep running on the same thread.', paths: ['python', 'javascript'], aliases: ['asynchronous', 'concurrency'] },
  { term: 'Big O notation', definition: 'A way to describe how an algorithm\'s cost grows with input size, ignoring constants. O(1) is constant, O(log n) barely grows, O(n) is proportional, O(n²) doubles the input and quadruples the work.', paths: ['dsa'], aliases: ['complexity', 'time complexity'] },
  { term: 'Binary', definition: 'Base 2: numbers written with only 0 and 1. Everything a computer stores is ultimately binary, which is why 8 bits (one byte) give 256 possible values.', paths: ['computer-architecture'], aliases: ['bit', 'byte'] },
  { term: 'Boolean', definition: 'A value that is either true or false. Comparisons produce booleans, and if statements consume them. Named after George Boole.', paths: ['python', 'javascript'] },
  { term: 'Branch (git)', definition: 'An independent line of commits. You create a branch to work on something without disturbing main, then merge it back when it is ready.', paths: ['git'] },
  { term: 'Bug', definition: 'A mistake in a program that makes it behave incorrectly. Reading the error message calmly is the first debugging step; the second is shrinking the input until the smallest failing case is clear.', paths: ['python'], aliases: ['debugging'] },
  { term: 'Cache', definition: 'A copy of data kept somewhere faster than its source, so the next request does not pay the full cost. CPUs, browsers, and functools.cache all use the same idea.', paths: ['computer-architecture', 'python'], aliases: ['memoisation', 'memoization'] },
  { term: 'Callback', definition: 'A function you hand to another function to be called later, when something happens or finishes. Event handlers and setTimeout use callbacks; promises were invented to tame them.', paths: ['javascript'] },
  { term: 'Class', definition: 'A blueprint for objects: it names the data each object holds and the methods that operate on it. An instance is one concrete object made from the class.', paths: ['python', 'javascript', 'java', 'csharp'], aliases: ['object-oriented', 'OOP', 'instance'] },
  { term: 'CLI', definition: 'Command-line interface: a program you run by typing its name and options into a terminal rather than clicking. Fast to automate and easy to combine with other tools.', paths: ['terminal', 'bash'], aliases: ['command line', 'shell'] },
  { term: 'Closure', definition: 'A function bundled with the variables from the scope it was created in. The inner function keeps those variables alive, which is how private state and counters work in JavaScript and Python.', paths: ['javascript', 'python'] },
  { term: 'Comment', definition: 'Text in source code the computer ignores, written for people. Good comments explain why, not what; the code already says what.', paths: ['python', 'javascript'] },
  { term: 'Commit (git)', definition: 'A saved snapshot of your project with a message describing the change. Commits form a history you can inspect, revert and share.', paths: ['git'] },
  { term: 'Compiler', definition: 'A program that translates source code into another form, typically machine code, before it runs. C, Rust and Go are compiled; the compiler catches many errors up front.', paths: ['compilers', 'c', 'rust'] },
  { term: 'Comprehension', definition: 'Python\'s compact syntax for building a list, set or dict from an iterable in one expression: [x * 2 for x in data if x > 0].', paths: ['python'] },
  { term: 'CSS', definition: 'Cascading Style Sheets: the language that controls how HTML looks — colours, spacing, layout, animation. It cascades because several rules can apply and the most specific wins.', paths: ['css', 'tailwind'] },
  { term: 'Database', definition: 'Software that stores structured data and answers questions about it reliably, even with many users at once. Relational databases organise data into tables and are queried with SQL.', paths: ['sql'] },
  { term: 'Dataclass', definition: 'A Python class declared with @dataclass that gets __init__, __repr__ and __eq__ generated from its field annotations. The default choice for simple records.', paths: ['python'] },
  { term: 'Decorator', definition: 'A function that takes a function and returns a new one, applied with the @name syntax. Used for caching, logging, timing and registering handlers without touching the original code.', paths: ['python'] },
  { term: 'Dependency', definition: 'Code your project needs that someone else wrote: a package from PyPI or npm. Recorded in requirements.txt, pyproject.toml or package.json so others can install the same versions.', paths: ['python', 'nodejs'], aliases: ['package', 'library'] },
  { term: 'Dictionary', definition: 'A collection that maps keys to values with fast lookup: phone book, not list. Called dict in Python, object or Map in JavaScript, hash map in general.', paths: ['python', 'javascript', 'dsa'], aliases: ['hash map', 'map', 'object'] },
  { term: 'DOM', definition: 'Document Object Model: the browser\'s live tree of objects representing the page. JavaScript changes what you see by changing the DOM.', paths: ['javascript', 'html'] },
  { term: 'Exception', definition: 'An error object raised when something goes wrong. Code can catch it with try/except (Python) or try/catch (JavaScript) and recover; uncaught, it stops the program with a traceback.', paths: ['python', 'javascript'], aliases: ['error', 'traceback', 'stack trace'] },
  { term: 'Function', definition: 'A named, reusable block of code that takes inputs, does something, and usually returns a result. The basic unit of organisation in almost every language.', paths: ['python', 'javascript'] },
  { term: 'Generator', definition: 'A function that yields values one at a time instead of returning a whole list, so it can produce huge or infinite sequences lazily. Python uses yield; JavaScript uses function*.', paths: ['python', 'javascript'], aliases: ['iterator', 'lazy'] },
  { term: 'Git', definition: 'The standard version-control system. It records every change to a project, lets many people work in parallel, and makes it safe to experiment because anything can be undone.', paths: ['git'], aliases: ['version control', 'GitHub'] },
  { term: 'HTML', definition: 'HyperText Markup Language: the structure of every web page, written as nested tags such as <h1>, <p> and <a>. Content lives in HTML, appearance in CSS, behaviour in JavaScript.', paths: ['html'] },
  { term: 'HTTP', definition: 'The protocol browsers and servers use to talk. A request names a method (GET, POST) and a URL; the response carries a status code (200 OK, 404 Not Found) and a body.', paths: ['internet', 'nodejs'], aliases: ['request', 'response', 'status code'] },
  { term: 'IDE', definition: 'Integrated Development Environment: an editor with a debugger, run button and language tooling built in. VS Code with extensions is the common middle ground.', paths: [], aliases: ['editor'] },
  { term: 'Immutable', definition: 'Cannot be changed after creation. Strings and tuples in Python are immutable; a method like upper() returns a new string. Immutability makes code easier to reason about.', paths: ['python', 'rust'], aliases: ['mutable'] },
  { term: 'Index', definition: 'Two meanings. In a sequence, the position of an element, counting from 0. In a database, a lookup structure that makes searching a column fast.', paths: ['python', 'sql'] },
  { term: 'Interpreter', definition: 'A program that runs source code directly, statement by statement, instead of compiling it first. Python and JavaScript are interpreted, which makes them quick to try and easy to script.', paths: ['python', 'compilers'] },
  { term: 'JSON', definition: 'JavaScript Object Notation: a text format for structured data using objects, arrays, strings, numbers, booleans and null. The default language of web APIs and config files.', paths: ['python', 'javascript'] },
  { term: 'Loop', definition: 'A statement that repeats a block: for iterates over a sequence, while repeats as long as a condition holds. break exits early; continue skips to the next round.', paths: ['python', 'javascript'] },
  { term: 'Module', definition: 'A file of code you can import into another. Python modules are .py files; a package is a directory of them. Modules keep names organised and code reusable.', paths: ['python', 'javascript'], aliases: ['import', 'package'] },
  { term: 'None / null', definition: 'The value that means "nothing here". Python has None, JavaScript has both null and undefined. Many crashes are a method called on one of these by accident.', paths: ['python', 'javascript'], aliases: ['undefined'] },
  { term: 'Operating system', definition: 'The software that manages the hardware and runs your programs: Linux, macOS, Windows. It provides files, processes, memory and networking through system calls.', paths: ['operating-systems'], aliases: ['kernel', 'process'] },
  { term: 'Pointer', definition: 'A variable holding a memory address. C and C++ use them explicitly; most other languages hide them behind references. Getting them wrong is the source of many security bugs.', paths: ['c', 'cpp', 'rust'], aliases: ['reference', 'memory'] },
  { term: 'Primary key', definition: 'The column (or columns) that uniquely identifies each row in a database table, usually an auto-incrementing id. Foreign keys in other tables point at it.', paths: ['sql'], aliases: ['foreign key'] },
  { term: 'Promise', definition: 'A JavaScript object representing a value that will arrive later. It resolves with a result or rejects with an error; await unwraps it.', paths: ['javascript'] },
  { term: 'Recursion', definition: 'A function that calls itself on a smaller version of the problem until a base case stops it. Natural for trees and nested data; a loop is better for flat sequences.', paths: ['python', 'dsa'] },
  { term: 'Regular expression', definition: 'A pattern language for matching text: \\d+ means "one or more digits". Available in every language; excellent for validation and extraction, terrible for parsing nested structures.', paths: ['python', 'javascript'], aliases: ['regex'] },
  { term: 'Repository', definition: 'A project tracked by Git, including its full history. A remote repository on GitHub is the shared copy everyone pushes to and pulls from.', paths: ['git'], aliases: ['repo'] },
  { term: 'Runtime', definition: 'Either the period when a program is executing ("a runtime error"), or the environment that executes it (the Python interpreter, Node.js, a browser).', paths: ['nodejs', 'python'] },
  { term: 'Scope', definition: 'The region of code where a name is visible. Variables defined inside a function are local to it; module-level names are global. Blocks create scope in JavaScript (let/const) but not in Python.', paths: ['python', 'javascript'] },
  { term: 'Shell', definition: 'The program that reads your typed commands and runs them: bash and zsh on Unix, PowerShell on Windows. Shell scripts automate sequences of commands.', paths: ['bash', 'powershell', 'terminal'], aliases: ['bash', 'zsh', 'terminal'] },
  { term: 'SQL', definition: 'Structured Query Language: how you ask a relational database questions and change its data. SELECT reads, INSERT/UPDATE/DELETE write, JOIN combines tables.', paths: ['sql'] },
  { term: 'Stack', definition: 'A last-in, first-out collection: push adds to the top, pop removes from it. The call stack tracks which functions are running; a stack overflow is recursion that never stopped.', paths: ['dsa', 'python'], aliases: ['queue', 'call stack'] },
  { term: 'String', definition: 'A sequence of characters — text. Immutable in Python and JavaScript; every method returns a new string. f-strings and template literals embed values inside them.', paths: ['python', 'javascript'] },
  { term: 'Syntax', definition: 'The grammar of a language: which arrangements of symbols are valid programs. A syntax error means the code could not even be parsed, so nothing ran.', paths: ['python', 'compilers'] },
  { term: 'Test', definition: 'Code that runs other code and checks the result. Automated tests catch regressions the moment they happen; pytest (Python) and Vitest (JavaScript) are the common runners.', paths: ['python', 'javascript'], aliases: ['unit test', 'pytest'] },
  { term: 'Type', definition: 'The kind of a value: integer, string, list, function. Static languages check types before running; dynamic ones check as they go. Type hints let Python have both.', paths: ['python', 'typescript'], aliases: ['type hint', 'static typing', 'dynamic typing'] },
  { term: 'Variable', definition: 'A name bound to a value. Assignment binds; later reads look the value up. In Python the name has no fixed type, so the same name can hold different kinds of value over time.', paths: ['python', 'javascript'] },
  { term: 'Virtual environment', definition: 'A private, per-project Python installation so different projects can use different package versions without conflict. Created with python -m venv .venv and activated in the shell.', paths: ['python'], aliases: ['venv', 'pip'] },
  { term: 'WebAssembly', definition: 'A binary instruction format browsers can run at near-native speed. It is how this site runs a full Python interpreter and SQLite inside your browser tab.', paths: ['internet', 'compilers'], aliases: ['wasm'] },
]
