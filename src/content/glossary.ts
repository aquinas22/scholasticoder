export interface GlossaryEntry {
  term: string
  definition: string
  /** Learning path slugs where the idea is taught. */
  paths?: string[]
  aliases?: string[]
  /** A short illustrative snippet shown in the popover. */
  example?: { code: string; language: string }
  /** Extra words that should link to this entry in lesson prose (in addition to term and aliases). */
  match?: string[]
  /** Set false for words too common to auto-link in prose (they still appear in the glossary). */
  linkable?: boolean
}

/** Plain-language definitions of the words that trip people up. Kept alphabetical. */
export const glossary: GlossaryEntry[] = [
  { term: 'Aggregate function', definition: 'A SQL function that collapses many rows into one value: COUNT, SUM, AVG, MIN, MAX. With GROUP BY it runs once per group.', paths: ['sql'], match: ['aggregate', 'aggregates', 'GROUP BY'], example: { code: "SELECT age, AVG(grade) AS avg_grade, COUNT(*) AS n\nFROM students\nGROUP BY age\nHAVING COUNT(*) > 1;", language: 'sql' } },
  { term: 'Algorithm', definition: 'A precise sequence of steps that turns an input into an output. Sorting a list, finding a route, and compressing an image are all algorithms; the same one can be written in any language.', paths: ['dsa', 'python'], example: { code: "def is_prime(n):\n    return n > 1 and all(n % d for d in range(2, int(n ** 0.5) + 1))\n\nprint([n for n in range(20) if is_prime(n)])", language: 'python' } },
  { term: 'API', definition: 'Application Programming Interface: the set of functions, endpoints or commands one piece of software exposes so another can use it. A web API is usually URLs that accept and return JSON.', paths: ['javascript', 'python', 'internet'], aliases: ['REST', 'endpoint'], example: { code: "import json, urllib.request\n\nwith urllib.request.urlopen(\"https://api.example.com/books/1\") as r:\n    book = json.load(r)   # the API answers with JSON\nprint(book[\"title\"])", language: 'python' } },
  { term: 'Argument', definition: 'A value you pass into a function when calling it. The names in the function definition are parameters; the values at the call site are arguments.', paths: ['python', 'javascript'], aliases: ['parameter'], example: { code: "def greet(name, greeting=\"Hello\"):   # parameters\n    return f\"{greeting}, {name}!\"\n\ngreet(\"Ada\", greeting=\"Salve\")        # arguments", language: 'python' } },
  { term: 'Array', definition: 'An ordered collection of values accessed by numeric index, starting at 0. Python calls its flexible version a list; C arrays are fixed-size blocks of memory.', paths: ['javascript', 'c', 'dsa'], aliases: ['list'], example: { code: "scores = [90, 85, 77]      # a Python list\nscores.append(100)\nprint(scores[0], len(scores))", language: 'python' } },
  { term: 'Arrow function', definition: 'JavaScript\'s short function syntax: (a, b) => a + b. Arrows have no this of their own, which makes them ideal for callbacks inside methods.', paths: ['javascript', 'typescript'], match: ['arrow functions', '=>'], example: { code: "const double = n => n * 2\nconst add = (a, b) => a + b\n[1, 2, 3].map(n => n * n)", language: 'javascript' } },
  { term: 'Assembly', definition: 'Human-readable names for a CPU\'s raw instructions. One line of assembly is one machine operation, which is why it is the last stop before the hardware.', paths: ['asm', 'computer-architecture'] },
  { term: 'assert', definition: 'A statement that checks a condition you believe must hold and raises AssertionError if it does not. Used in tests and as a sanity check during development.', paths: ['python'], example: { code: "def split_bill(total, people):\n    assert people > 0, \"need at least one person\"\n    return total / people", language: 'python' } },
  { term: 'Async / await', definition: 'Syntax for writing code that waits on slow operations (network, disk) without blocking everything else. await pauses one task; other tasks keep running on the same thread.', paths: ['python', 'javascript'], aliases: ['asynchronous', 'concurrency'], example: { code: "import asyncio\n\nasync def fetch(name, delay):\n    await asyncio.sleep(delay)\n    return name\n\nasync def main():\n    print(await asyncio.gather(fetch(\"a\", 0.2), fetch(\"b\", 0.1)))\n\nasyncio.run(main())", language: 'python' } },
  { term: 'Big O notation', definition: 'A way to describe how an algorithm\'s cost grows with input size, ignoring constants. O(1) is constant, O(log n) barely grows, O(n) is proportional, O(n²) doubles the input and quadruples the work.', paths: ['dsa'], aliases: ['complexity', 'time complexity'], example: { code: "# O(n): one pass\ntotal = sum(numbers)\n\n# O(n^2): a pass inside a pass\npairs = [(a, b) for a in numbers for b in numbers]\n\n# O(log n): halving each step\nimport bisect\ni = bisect.bisect_left(sorted_numbers, 42)", language: 'python' } },
  { term: 'Binary', definition: 'Base 2: numbers written with only 0 and 1. Everything a computer stores is ultimately binary, which is why 8 bits (one byte) give 256 possible values.', paths: ['computer-architecture'], aliases: ['bit', 'byte'] },
  { term: 'Boolean', definition: 'A value that is either true or false. Comparisons produce booleans, and if statements consume them. Named after George Boole.', paths: ['python', 'javascript'], example: { code: "is_open = 9 <= hour < 17\nprint(type(is_open), is_open and not holiday)", language: 'python' } },
  { term: 'Branch (git)', definition: 'An independent line of commits. You create a branch to work on something without disturbing main, then merge it back when it is ready.', paths: ['git'], example: { code: "git switch -c feature/psalter\n# ...work, commit...\ngit switch main\ngit merge feature/psalter", language: 'bash' } },
  { term: 'break and continue', definition: 'Two loop controls. break leaves the loop entirely; continue skips the rest of the current iteration and moves to the next one.', paths: ['python', 'javascript'], match: ['break', 'continue statement'], example: { code: "for n in range(10):\n    if n == 7:\n        break        # stop the whole loop\n    if n % 2:\n        continue     # skip odd numbers\n    print(n)         # 0 2 4 6", language: 'python' } },
  { term: 'Bug', definition: 'A mistake in a program that makes it behave incorrectly. Reading the error message calmly is the first debugging step; the second is shrinking the input until the smallest failing case is clear.', paths: ['python'], aliases: ['debugging'] },
  { term: 'Cache', definition: 'A copy of data kept somewhere faster than its source, so the next request does not pay the full cost. CPUs, browsers, and functools.cache all use the same idea.', paths: ['computer-architecture', 'python'], aliases: ['memoisation', 'memoization'], example: { code: "from functools import cache\n\n@cache\ndef fib(n):\n    return n if n < 2 else fib(n - 1) + fib(n - 2)\n\nprint(fib(90))   # instant: each n computed once", language: 'python' } },
  { term: 'Callback', definition: 'A function you hand to another function to be called later, when something happens or finishes. Event handlers and setTimeout use callbacks; promises were invented to tame them.', paths: ['javascript'], example: { code: "button.addEventListener('click', () => {\n  console.log('clicked')   // this arrow function is the callback\n})\nsetTimeout(() => console.log('later'), 500)", language: 'javascript' } },
  { term: 'Class', definition: 'A blueprint for objects: it names the data each object holds and the methods that operate on it. An instance is one concrete object made from the class.', paths: ['python', 'javascript', 'java', 'csharp'], aliases: ['object-oriented', 'OOP', 'instance'], example: { code: "class Account:\n    def __init__(self, owner):\n        self.owner = owner\n        self.balance = 0\n\n    def deposit(self, amount):\n        self.balance += amount\n\nacct = Account(\"Ada\")   # an instance\nacct.deposit(50)", language: 'python' } },
  { term: 'CLI', definition: 'Command-line interface: a program you run by typing its name and options into a terminal rather than clicking. Fast to automate and easy to combine with other tools.', paths: ['terminal', 'bash'], aliases: ['command line', 'shell'] },
  { term: 'Closure', definition: 'A function bundled with the variables from the scope it was created in. The inner function keeps those variables alive, which is how private state and counters work in JavaScript and Python.', paths: ['javascript', 'python'], example: { code: "def make_counter():\n    count = 0\n    def bump():\n        nonlocal count\n        count += 1\n        return count\n    return bump\n\nc = make_counter()\nprint(c(), c())   # 1 2 \u2014 count lives on inside bump", language: 'python' } },
  { term: 'Comment', definition: 'Text in source code the computer ignores, written for people. Good comments explain why, not what; the code already says what.', paths: ['python', 'javascript'] },
  { term: 'Commit (git)', definition: 'A saved snapshot of your project with a message describing the change. Commits form a history you can inspect, revert and share.', paths: ['git'], example: { code: "git add .\ngit commit -m \"Add the psalter index\"\ngit log --oneline", language: 'bash' } },
  { term: 'Compiler', definition: 'A program that translates source code into another form, typically machine code, before it runs. C, Rust and Go are compiled; the compiler catches many errors up front.', paths: ['compilers', 'c', 'rust'] },
  { term: 'Comprehension', definition: 'Python\'s compact syntax for building a list, set or dict from an iterable in one expression: [x * 2 for x in data if x > 0].', paths: ['python'], example: { code: "squares = [n * n for n in range(10) if n % 2 == 0]\nlengths = {w: len(w) for w in [\"ora\", \"labora\"]}", language: 'python' } },
  { term: 'const and let', definition: 'Modern JavaScript declarations. const binds a name that cannot be reassigned; let can be. Both are block-scoped. Avoid var.', paths: ['javascript', 'typescript'], match: ['const', 'let'], linkable: false, example: { code: "const limit = 10      // cannot be reassigned\nlet count = 0         // can\ncount += 1", language: 'javascript' } },
  { term: 'CSS', definition: 'Cascading Style Sheets: the language that controls how HTML looks — colours, spacing, layout, animation. It cascades because several rules can apply and the most specific wins.', paths: ['css', 'tailwind'], example: { code: ".card { padding: 1rem; border-radius: 8px; }\n.card h1 { color: darkred; font-family: Georgia, serif; }", language: 'css' } },
  { term: 'Database', definition: 'Software that stores structured data and answers questions about it reliably, even with many users at once. Relational databases organise data into tables and are queried with SQL.', paths: ['sql'] },
  { term: 'Dataclass', definition: 'A Python class declared with @dataclass that gets __init__, __repr__ and __eq__ generated from its field annotations. The default choice for simple records.', paths: ['python'], example: { code: "from dataclasses import dataclass\n\n@dataclass\nclass Point:\n    x: float\n    y: float\n\nprint(Point(1, 2) == Point(1, 2))   # True, for free", language: 'python' } },
  { term: 'Decorator', definition: 'A function that takes a function and returns a new one, applied with the @name syntax. Used for caching, logging, timing and registering handlers without touching the original code.', paths: ['python'], example: { code: "import functools\n\ndef shout(func):\n    @functools.wraps(func)\n    def wrapper(*a, **kw):\n        return func(*a, **kw).upper()\n    return wrapper\n\n@shout\ndef greet(name):\n    return f\"hello {name}\"\n\nprint(greet(\"bede\"))   # HELLO BEDE", language: 'python' } },
  { term: 'def', definition: 'The Python keyword that defines a function. The name comes next, then the parameters in parentheses, then a colon and an indented body.', paths: ['python'], example: { code: "def hypotenuse(a, b):\n    return (a ** 2 + b ** 2) ** 0.5", language: 'python' } },
  { term: 'Dependency', definition: 'Code your project needs that someone else wrote: a package from PyPI or npm. Recorded in requirements.txt, pyproject.toml or package.json so others can install the same versions.', paths: ['python', 'nodejs'], aliases: ['package', 'library'] },
  { term: 'Destructuring', definition: 'Unpacking values from arrays or objects into variables in one statement, with defaults and rest collection.', paths: ['javascript', 'typescript'], example: { code: "const { host, port = 8080, ...rest } = config\nconst [first, ...others] = list", language: 'javascript' } },
  { term: 'Dictionary', definition: 'A collection that maps keys to values with fast lookup: phone book, not list. Called dict in Python, object or Map in JavaScript, hash map in general.', paths: ['python', 'javascript', 'dsa'], aliases: ['hash map'], example: { code: "ages = {\"Bede\": 62, \"Hild\": 66}\nages[\"Alcuin\"] = 69\nprint(ages.get(\"Ada\", \"unknown\"), list(ages))", language: 'python' } },
  { term: 'Docstring', definition: 'A string literal placed first inside a function, class or module that documents it. help() and editors read it; it lives on the __doc__ attribute.', paths: ['python'], example: { code: "def area(r):\n    \"\"\"Return the area of a circle of radius r.\"\"\"\n    return 3.14159 * r * r\n\nprint(area.__doc__)", language: 'python' } },
  { term: 'DOM', definition: 'Document Object Model: the browser\'s live tree of objects representing the page. JavaScript changes what you see by changing the DOM.', paths: ['javascript', 'html'], example: { code: "const h1 = document.querySelector('h1')\nh1.textContent = 'Changed by JavaScript'\ndocument.body.append(document.createElement('p'))", language: 'javascript' } },
  { term: 'Duck typing', definition: 'Caring about what an object can do rather than what it is. If it has the methods you need, it works — the name comes from "if it quacks like a duck".', paths: ['python'], example: { code: "def total_length(items):\n    return sum(len(x) for x in items)   # anything with len() works\n\nprint(total_length([\"abbey\", [1, 2], {\"a\": 1}]))", language: 'python' } },
  { term: 'Enumerate', definition: 'A built-in that pairs each item of an iterable with its index, so you loop over both at once without managing a counter.', paths: ['python'], match: ['enumerate'], example: { code: "for i, monk in enumerate([\"Bede\", \"Hild\"], start=1):\n    print(i, monk)", language: 'python' } },
  { term: 'Environment variable', definition: 'A named value every program inherits from its parent: HOME, PATH, USER. export sets one for the current shell and its children.', paths: ['terminal', 'bash'], match: ['environment variables', 'PATH'], example: { code: "echo $HOME\nexport EDITOR=nano\nenv | grep EDITOR", language: 'bash' } },
  { term: 'Event loop', definition: 'The mechanism that lets a single thread handle many waiting operations: it runs a task, and when the task awaits something slow, it picks up the next ready task. asyncio and the browser both work this way.', paths: ['javascript', 'python', 'nodejs'], example: { code: "console.log('one')\nsetTimeout(() => console.log('three'), 0)\nconsole.log('two')   // the timeout waits for the loop to come round", language: 'javascript' } },
  { term: 'Exception', definition: 'An error object raised when something goes wrong. Code can catch it with try/except (Python) or try/catch (JavaScript) and recover; uncaught, it stops the program with a traceback.', paths: ['python', 'javascript'], aliases: ['error', 'traceback', 'stack trace'], example: { code: "try:\n    n = int(\"forty\")\nexcept ValueError as e:\n    print(\"bad number:\", e)\nfinally:\n    print(\"always runs\")", language: 'python' } },
  { term: 'Exit code', definition: 'The number a program returns when it finishes: 0 means success, anything else means failure. The shell stores the last one in $? and uses it for && and ||.', paths: ['terminal', 'bash'], aliases: ['exit status'], match: ['$?'], example: { code: "ls /nope\necho $?          # 2\nmake && ./run    # run only if make succeeded", language: 'bash' } },
  { term: 'f-string', definition: 'A string literal prefixed with f whose {braces} evaluate expressions inline, with an optional format spec after a colon. The modern way to build text in Python.', paths: ['python'], aliases: ['formatted string literal'], match: ['f-strings'], example: { code: "name, price = \"vellum\", 24.5\nprint(f\"{name:<10}{price:>8.2f}\")\nprint(f\"{name=}\")", language: 'python' } },
  { term: 'Function', definition: 'A named, reusable block of code that takes inputs, does something, and usually returns a result. The basic unit of organisation in almost every language.', paths: ['python', 'javascript'], example: { code: "def area(width, height=1):\n    \"\"\"Return the area of a rectangle.\"\"\"\n    return width * height\n\nprint(area(3, 4), area(5))", language: 'python' } },
  { term: 'Generator', definition: 'A function that yields values one at a time instead of returning a whole list, so it can produce huge or infinite sequences lazily. Python uses yield; JavaScript uses function*.', paths: ['python', 'javascript'], aliases: ['iterator', 'lazy'], example: { code: "def countdown(n):\n    while n > 0:\n        yield n\n        n -= 1\n\nprint(list(countdown(3)))   # [3, 2, 1]", language: 'python' } },
  { term: 'Generic', definition: 'A type parameter that lets a function or class work with many types while staying type-safe: Stack<T> is a stack of whatever T you choose.', paths: ['typescript', 'rust', 'java'], match: ['generics'], example: { code: "function firstOr<T>(items: T[], fallback: T): T {\n  return items.length ? items[0] : fallback\n}\nfirstOr([1, 2], 0)          // T is number\nfirstOr([], 'none')         // T is string", language: 'typescript' } },
  { term: 'Git', definition: 'The standard version-control system. It records every change to a project, lets many people work in parallel, and makes it safe to experiment because anything can be undone.', paths: ['git'], aliases: ['version control', 'GitHub'] },
  { term: 'Global and nonlocal', definition: 'Keywords that let a function assign to a variable from an enclosing scope instead of creating a new local one. nonlocal reaches the enclosing function; global reaches module level. Use sparingly.', paths: ['python'], match: ['nonlocal', 'global variable'], example: { code: "def make_counter():\n    count = 0\n    def bump():\n        nonlocal count\n        count += 1\n        return count\n    return bump", language: 'python' } },
  { term: 'Hoisting', definition: 'JavaScript moves function declarations (and var declarations, without their values) to the top of their scope before running. let and const are not hoisted the same way, which is one reason to prefer them.', paths: ['javascript'], example: { code: "greet()                // works: function declarations are hoisted\nfunction greet() { console.log('hi') }\n\nconsole.log(x)         // undefined, not an error, because of var hoisting\nvar x = 1", language: 'javascript' } },
  { term: 'HTML', definition: 'HyperText Markup Language: the structure of every web page, written as nested tags such as <h1>, <p> and <a>. Content lives in HTML, appearance in CSS, behaviour in JavaScript.', paths: ['html'], example: { code: "<article>\n  <h1>Ora et labora</h1>\n  <p>The <a href=\"/rule\">Rule</a> in one line.</p>\n</article>", language: 'html' } },
  { term: 'HTTP', definition: 'The protocol browsers and servers use to talk. A request names a method (GET, POST) and a URL; the response carries a status code (200 OK, 404 Not Found) and a body.', paths: ['internet', 'nodejs'], aliases: ['request', 'response', 'status code'], example: { code: "GET /books/1 HTTP/1.1\nHost: api.example.com\n\nHTTP/1.1 200 OK\nContent-Type: application/json\n\n{\"title\": \"Historia Ecclesiastica\"}", language: 'text' } },
  { term: 'IDE', definition: 'Integrated Development Environment: an editor with a debugger, run button and language tooling built in. VS Code with extensions is the common middle ground.', paths: [], aliases: ['editor'], linkable: false },
  { term: 'Immutable', definition: 'Cannot be changed after creation. Strings and tuples in Python are immutable; a method like upper() returns a new string. Immutability makes code easier to reason about.', paths: ['python', 'rust'], aliases: ['mutable'], example: { code: "s = \"abbey\"\nt = s.upper()      # a new string; s is unchanged\npoint = (3, 4)\n# point[0] = 5  -> TypeError: tuples cannot be changed", language: 'python' } },
  { term: 'Indentation', definition: 'Python uses leading whitespace to mark blocks: the body of an if, loop, function or class is everything indented beneath it. Four spaces is the convention; mixing tabs and spaces is an error.', paths: ['python'], match: ['indented', 'indent'], example: { code: "if hour < 12:\n    print(\"matins\")      # inside the if\n    print(\"lauds\")\nprint(\"always\")          # outside", language: 'python' } },
  { term: 'Index', definition: 'Two meanings. In a sequence, the position of an element, counting from 0. In a database, a lookup structure that makes searching a column fast.', paths: ['python', 'sql'], example: { code: "names = [\"Bede\", \"Hild\"]\nprint(names[0], names[-1])   # positions count from 0\n\n-- SQL: CREATE INDEX idx_email ON users(email);", language: 'python' }, linkable: false },
  { term: 'Interface', definition: 'A named description of an object\'s shape in TypeScript: which properties it has and their types. Interfaces can be extended and merged; they vanish at compile time.', paths: ['typescript'], match: ['interfaces'], example: { code: "interface Monk {\n  name: string\n  age: number\n  skills?: string[]\n}\nconst bede: Monk = { name: 'Bede', age: 62 }", language: 'typescript' } },
  { term: 'Interpreter', definition: 'A program that runs source code directly, statement by statement, instead of compiling it first. Python and JavaScript are interpreted, which makes them quick to try and easy to script.', paths: ['python', 'compilers'] },
  { term: 'Iterable and iterator', definition: 'An iterable is anything a for loop can walk over (lists, strings, dicts, files, generators). An iterator is the object that hands out the items one at a time via next().', paths: ['python'], aliases: ['iterator protocol'], match: ['iterable', 'iterator'], example: { code: "it = iter([\"ora\", \"labora\"])\nprint(next(it))   # ora\nprint(next(it))   # labora\n# next(it) -> StopIteration", language: 'python' } },
  { term: 'JOIN', definition: 'Combines rows from two tables where a condition matches, usually a foreign key equalling a primary key. INNER keeps matches only; LEFT keeps every row from the left table.', paths: ['sql'], match: ['joins', 'LEFT JOIN', 'INNER JOIN'], example: { code: "SELECT s.name, c.name AS course, e.score\nFROM enrollments e\nJOIN students s ON s.id = e.student_id\nJOIN courses  c ON c.id = e.course_id;", language: 'sql' } },
  { term: 'JSON', definition: 'JavaScript Object Notation: a text format for structured data using objects, arrays, strings, numbers, booleans and null. The default language of web APIs and config files.', paths: ['python', 'javascript'], example: { code: "import json\ntext = '{\"name\": \"Bede\", \"works\": 3, \"saint\": true}'\ndata = json.loads(text)\nprint(data[\"works\"], json.dumps(data, indent=2))", language: 'python' } },
  { term: 'Keyword argument', definition: 'An argument passed by name rather than position: greet(name=\"Ada\"). Keyword arguments make calls readable and let you skip defaults you do not want to change.', paths: ['python'], aliases: ['kwargs'], match: ['keyword arguments', '**kwargs'], example: { code: "def order(item, qty=1, gift_wrap=False):\n    ...\n\norder(\"candles\", gift_wrap=True)   # qty keeps its default", language: 'python' } },
  { term: 'Lambda', definition: 'An anonymous one-expression function. Handy as a sort key or a short callback; anything longer deserves a def with a name.', paths: ['python'], match: ['lambda'], example: { code: "monks = [(\"Bede\", 412), (\"Hild\", 88)]\nmonks.sort(key=lambda m: m[1])\nprint(monks)", language: 'python' } },
  { term: 'List', definition: 'Python\'s ordered, mutable sequence. Append, insert, slice, sort in place, and index from 0 (or from the end with negatives).', paths: ['python'], match: ['lists'], example: { code: "psalms = [23, 51, 100]\npsalms.append(150)\nprint(psalms[0], psalms[-1], psalms[1:3])", language: 'python' } },
  { term: 'Loop', definition: 'A statement that repeats a block: for iterates over a sequence, while repeats as long as a condition holds. break exits early; continue skips to the next round.', paths: ['python', 'javascript'], example: { code: "for word in [\"ora\", \"et\", \"labora\"]:\n    print(word)\n\nn = 3\nwhile n > 0:\n    n -= 1", language: 'python' } },
  { term: 'Method', definition: 'A function that belongs to an object and is called with dot notation: text.upper(), list.append(x). Inside a class, methods receive the instance as self.', paths: ['python', 'javascript'], match: ['methods'], example: { code: "class Bell:\n    def ring(self, times=1):\n        return \"dong \" * times\n\nprint(Bell().ring(3))   # Bell().ring is a method", language: 'python' } },
  { term: 'Module', definition: 'A file of code you can import into another. Python modules are .py files; a package is a directory of them. Modules keep names organised and code reusable.', paths: ['python', 'javascript'], aliases: ['import', 'package'], example: { code: "import math\nfrom datetime import date\n\nprint(math.sqrt(2), date.today().year)", language: 'python' } },
  { term: 'Mutable', definition: 'Can be changed in place. Lists, dicts and sets are mutable; strings, tuples and numbers are not. Mutable default arguments are a classic trap because they are shared between calls.', paths: ['python'], example: { code: "a = [1, 2]\nb = a           # same list, two names\nb.append(3)\nprint(a)        # [1, 2, 3] — a changed too", language: 'python' } },
  { term: 'Namespace', definition: 'A mapping from names to objects: each module, function call and class has its own. That is why two modules can both define a function called run without colliding.', paths: ['python'], example: { code: "import math\nimport statistics\n\nmath.sqrt(9)          # sqrt lives in the math namespace\nstatistics.mean([1, 2])", language: 'python' } },
  { term: 'Narrowing', definition: 'TypeScript refining a broad type to a specific one inside a branch, using typeof, instanceof, in, equality checks or a custom type guard.', paths: ['typescript'], match: ['type guard'], example: { code: "function describe(v: string | number) {\n  if (typeof v === 'string') return v.toUpperCase()   // v is string here\n  return v.toFixed(2)                                  // v is number here\n}", language: 'typescript' } },
  { term: 'None / null', definition: 'The value that means "nothing here". Python has None, JavaScript has both null and undefined. Many crashes are a method called on one of these by accident.', paths: ['python', 'javascript'], aliases: ['undefined'], example: { code: "result = None\nif result is None:\n    print(\"nothing yet\")\n\n# JavaScript: let x = null; let y;  // y is undefined", language: 'python' } },
  { term: 'NULL', definition: 'SQL\'s marker for a missing value. It is not equal to anything, including itself, so you test it with IS NULL. Aggregates skip it; COALESCE replaces it.', paths: ['sql'], example: { code: "SELECT name FROM students WHERE email IS NULL;\nSELECT COALESCE(email, 'no email') FROM students;", language: 'sql' } },
  { term: 'Operating system', definition: 'The software that manages the hardware and runs your programs: Linux, macOS, Windows. It provides files, processes, memory and networking through system calls.', paths: ['operating-systems'], aliases: ['kernel', 'process'] },
  { term: 'Package manager', definition: 'A tool that installs libraries and records which versions your project uses: pip for Python, npm for JavaScript, cargo for Rust. A lock file makes installs reproducible.', paths: ['python', 'nodejs'], aliases: ['pip', 'npm'], match: ['pip install'], example: { code: "pip install requests\npip freeze > requirements.txt\n\nnpm install express\nnpm ci   # exact versions from package-lock.json", language: 'bash' } },
  { term: 'pass', definition: 'A statement that does nothing. Python needs a body after a colon, so pass fills the gap while you sketch a function or class.', paths: ['python'], linkable: false, example: { code: "class Placeholder:\n    pass\n\ndef todo():\n    pass", language: 'python' } },
  { term: 'Pipe', definition: 'The | operator in a shell: it connects the output of one command to the input of the next, so small tools compose into pipelines.', paths: ['terminal', 'bash'], match: ['pipes', 'pipeline'], example: { code: "cat data/monks.csv | cut -d , -f 1 | sort | uniq -c", language: 'bash' } },
  { term: 'Pointer', definition: 'A variable holding a memory address. C and C++ use them explicitly; most other languages hide them behind references. Getting them wrong is the source of many security bugs.', paths: ['c', 'cpp', 'rust'], aliases: ['reference', 'memory'] },
  { term: 'Primary key', definition: 'The column (or columns) that uniquely identifies each row in a database table, usually an auto-incrementing id. Foreign keys in other tables point at it.', paths: ['sql'], aliases: ['foreign key'], example: { code: "CREATE TABLE students (\n  id INTEGER PRIMARY KEY,\n  name TEXT NOT NULL\n);\nCREATE TABLE enrollments (\n  student_id INTEGER REFERENCES students(id)\n);", language: 'sql' } },
  { term: 'Promise', definition: 'A JavaScript object representing a value that will arrive later. It resolves with a result or rejects with an error; await unwraps it.', paths: ['javascript'], example: { code: "const wait = ms => new Promise(resolve => setTimeout(resolve, ms))\n\nasync function main() {\n  await wait(100)\n  console.log('done')\n}\nmain()", language: 'javascript' } },
  { term: 'Property', definition: 'A method that is accessed like an attribute, declared with @property. It lets a class compute or validate a value while callers write obj.value without parentheses.', paths: ['python'], match: ['@property'], example: { code: "class Circle:\n    def __init__(self, r):\n        self.r = r\n\n    @property\n    def area(self):\n        return 3.14159 * self.r ** 2\n\nprint(Circle(2).area)   # no parentheses", language: 'python' } },
  { term: 'Prototype', definition: 'The object a JavaScript object delegates to when a property is missing. class syntax builds prototype chains for you; instance.method looks up Class.prototype.method.', paths: ['javascript'], match: ['prototypes'], example: { code: "class Bell { ring() { return 'dong' } }\nconst b = new Bell()\nconsole.log(Object.getPrototypeOf(b) === Bell.prototype)   // true", language: 'javascript' } },
  { term: 'Recursion', definition: 'A function that calls itself on a smaller version of the problem until a base case stops it. Natural for trees and nested data; a loop is better for flat sequences.', paths: ['python', 'dsa'], example: { code: "def factorial(n):\n    if n <= 1:          # base case\n        return 1\n    return n * factorial(n - 1)\n\nprint(factorial(5))", language: 'python' } },
  { term: 'Redirection', definition: 'Sending a command\'s output to a file instead of the screen: > overwrites, >> appends, 2> captures errors, < feeds a file in as input.', paths: ['terminal', 'bash'], match: ['redirect', 'stdout', 'stderr', 'stdin'], example: { code: "ls -l > files.txt\necho \"done\" >> files.txt\ncat missing 2> errors.log\nsort < names.txt", language: 'bash' } },
  { term: 'Regular expression', definition: 'A pattern language for matching text: \\d+ means "one or more digits". Available in every language; excellent for validation and extraction, terrible for parsing nested structures.', paths: ['python', 'javascript'], aliases: ['regex'], example: { code: "import re\nprint(re.findall(r\"\\d{4}-\\d{2}-\\d{2}\", \"born 0673-01-01, died 0735-05-26\"))", language: 'python' } },
  { term: 'Repository', definition: 'A project tracked by Git, including its full history. A remote repository on GitHub is the shared copy everyone pushes to and pulls from.', paths: ['git'], aliases: ['repo'] },
  { term: 'Return', definition: 'Ends a function and hands a value back to the caller. A function with no return (or a bare return) gives back None in Python and undefined in JavaScript.', paths: ['python', 'javascript'], linkable: false, example: { code: "def sign(n):\n    if n < 0:\n        return \"negative\"\n    return \"non-negative\"   # unreachable when n < 0", language: 'python' } },
  { term: 'Runtime', definition: 'Either the period when a program is executing ("a runtime error"), or the environment that executes it (the Python interpreter, Node.js, a browser).', paths: ['nodejs', 'python'], linkable: false },
  { term: 'Scope', definition: 'The region of code where a name is visible. Variables defined inside a function are local to it; module-level names are global. Blocks create scope in JavaScript (let/const) but not in Python.', paths: ['python', 'javascript'], example: { code: "total = 0            # module scope\n\ndef add(n):\n    result = total + n   # result is local to add\n    return result\n\nprint(add(5))\n# print(result) -> NameError: only exists inside add", language: 'python' } },
  { term: 'self', definition: 'The conventional name for the first parameter of a method: the instance the method was called on. Python passes it automatically; you must declare it.', paths: ['python'], example: { code: "class Monk:\n    def __init__(self, name):\n        self.name = name         # store on this instance\n\n    def greet(self):\n        return f\"Pax, {self.name}\"\n\nMonk(\"Bede\").greet()   # Python passes the instance as self", language: 'python' } },
  { term: 'Set', definition: 'An unordered collection of unique values with fast membership tests and set algebra: union, intersection, difference.', paths: ['python'], match: ['sets'], example: { code: "choir = {\"Bede\", \"Hild\", \"Ebba\"}\nkitchen = {\"Hild\", \"Oswald\"}\nprint(choir & kitchen, choir | kitchen, \"Bede\" in choir)", language: 'python' } },
  { term: 'Shell', definition: 'The program that reads your typed commands and runs them: bash and zsh on Unix, PowerShell on Windows. Shell scripts automate sequences of commands.', paths: ['bash', 'powershell', 'terminal'], aliases: ['bash', 'zsh', 'terminal'], example: { code: "ls -l | grep \".txt\" | wc -l\necho \"$HOME\"\ncat missing.txt 2> errors.log", language: 'bash' } },
  { term: 'Slice', definition: 'A sub-sequence taken with start:stop:step inside square brackets. The stop index is excluded; negative steps reverse.', paths: ['python'], match: ['slicing', 'slices'], example: { code: "s = \"scriptorium\"\nprint(s[0:6], s[-3:], s[::2], s[::-1])", language: 'python' } },
  { term: 'Spread and rest', definition: 'The same three dots doing opposite jobs. Spread expands an array or object into pieces; rest collects leftover pieces into an array or object.', paths: ['javascript', 'typescript'], match: ['spread', 'rest parameters'], example: { code: "const merged = { ...defaults, ...overrides }   // spread\nfunction sum(...nums) {                          // rest\n  return nums.reduce((t, n) => t + n, 0)\n}", language: 'javascript' } },
  { term: 'SQL', definition: 'Structured Query Language: how you ask a relational database questions and change its data. SELECT reads, INSERT/UPDATE/DELETE write, JOIN combines tables.', paths: ['sql'], example: { code: "SELECT name, grade\nFROM students\nWHERE age = 17\nORDER BY grade DESC\nLIMIT 3;", language: 'sql' } },
  { term: 'Stack', definition: 'A last-in, first-out collection: push adds to the top, pop removes from it. The call stack tracks which functions are running; a stack overflow is recursion that never stopped.', paths: ['dsa', 'python'], aliases: ['queue', 'call stack'], example: { code: "stack = []\nstack.append(\"a\")   # push\nstack.append(\"b\")\nprint(stack.pop())    # b \u2014 last in, first out", language: 'python' } },
  { term: 'Standard library', definition: 'The modules that ship with the language itself: math, json, re, datetime, pathlib and hundreds more in Python. No installation needed — just import.', paths: ['python'], aliases: ['stdlib'], example: { code: "import json, re, datetime, pathlib, collections, itertools, statistics\nprint(statistics.median([3, 1, 2]))", language: 'python' } },
  { term: 'Strict equality', definition: 'The === operator compares value and type without converting anything. Loose == coerces first, which makes 0 == "" true. Use === by default.', paths: ['javascript', 'typescript'], match: ['==='], example: { code: "console.log(0 == '', 0 === '')       // true false\nconsole.log(null == undefined, null === undefined)   // true false", language: 'javascript' } },
  { term: 'String', definition: 'A sequence of characters — text. Immutable in Python and JavaScript; every method returns a new string. f-strings and template literals embed values inside them.', paths: ['python', 'javascript'], example: { code: "name = \"Bede\"\nprint(name.upper(), name[0], len(name), f\"{name} of Jarrow\")", language: 'python' } },
  { term: 'Subquery', definition: 'A SELECT nested inside another statement. It can return one value, a list for IN, or a whole derived table in FROM. A CTE (WITH ...) names one so the query reads in order.', paths: ['sql'], aliases: ['CTE', 'common table expression'], match: ['subqueries', 'WITH clause'], example: { code: "WITH big AS (\n  SELECT customer_id FROM orders WHERE total > 500\n)\nSELECT name FROM customers WHERE id IN (SELECT customer_id FROM big);", language: 'sql' } },
  { term: 'Syntax', definition: 'The grammar of a language: which arrangements of symbols are valid programs. A syntax error means the code could not even be parsed, so nothing ran.', paths: ['python', 'compilers'], linkable: false },
  { term: 'Template literal', definition: 'A JavaScript string in backticks that can span lines and embed expressions with ${...}. The counterpart of Python\'s f-string.', paths: ['javascript', 'typescript'], match: ['template literals', 'backticks'], example: { code: "const name = 'Hild'\nconsole.log(`${name} has ${2 + 3} books`)", language: 'javascript' } },
  { term: 'Test', definition: 'Code that runs other code and checks the result. Automated tests catch regressions the moment they happen; pytest (Python) and Vitest (JavaScript) are the common runners.', paths: ['python', 'javascript'], aliases: ['unit test', 'pytest'], example: { code: "def add(a, b):\n    return a + b\n\ndef test_add():\n    assert add(2, 3) == 5\n    assert add(-1, 1) == 0\n# run with: pytest", language: 'python' }, linkable: false },
  { term: 'this', definition: 'In JavaScript, the object a function was called on — decided at call time, not where the function was written. Arrow functions capture the surrounding this instead.', paths: ['javascript'], linkable: false, example: { code: "const timer = {\n  ticks: 0,\n  start() { setTimeout(() => this.ticks++, 10) }   // arrow keeps this\n}", language: 'javascript' } },
  { term: 'Transaction', definition: 'A group of SQL statements that succeed or fail together. BEGIN starts it, COMMIT makes it permanent, ROLLBACK undoes it. This is how a bank transfer never loses money halfway.', paths: ['sql'], match: ['transactions', 'COMMIT', 'ROLLBACK'], example: { code: "BEGIN TRANSACTION;\nUPDATE accounts SET balance = balance - 100 WHERE id = 1;\nUPDATE accounts SET balance = balance + 100 WHERE id = 2;\nCOMMIT;", language: 'sql' } },
  { term: 'Tuple', definition: 'An immutable ordered sequence, written with parentheses (or just commas). Good for fixed records, multiple return values and dictionary keys.', paths: ['python'], match: ['tuples'], example: { code: "point = (3, 4)\nx, y = point                 # unpacking\ndef min_max(xs):\n    return min(xs), max(xs)  # returns a tuple", language: 'python' } },
  { term: 'Type', definition: 'The kind of a value: integer, string, list, function. Static languages check types before running; dynamic ones check as they go. Type hints let Python have both.', paths: ['python', 'typescript'], aliases: ['type hint', 'static typing', 'dynamic typing'], example: { code: "print(type(42), type(\"42\"), type([]))\n\ndef double(n: int) -> int:   # a type hint\n    return n * 2", language: 'python' }, linkable: false },
  { term: 'Type hint', definition: 'An annotation that states the intended type of a variable, parameter or return value. Python ignores it at runtime; tools such as mypy, pyright and your editor use it to catch mistakes early.', paths: ['python', 'typescript'], aliases: ['annotation'], match: ['type hints', 'annotations'], example: { code: "def total(prices: list[float], tax: float = 0.0) -> float:\n    return sum(prices) * (1 + tax)", language: 'python' } },
  { term: 'Union type', definition: 'A type that is one of several alternatives, written with |. Discriminated unions add a literal tag so the compiler can narrow inside a switch.', paths: ['typescript'], match: ['union', 'discriminated union'], example: { code: "type Payment =\n  | { kind: 'card'; last4: string }\n  | { kind: 'cash' }\n\nfunction label(p: Payment) {\n  return p.kind === 'card' ? `Card ${p.last4}` : 'Cash'\n}", language: 'typescript' } },
  { term: 'Variable', definition: 'A name bound to a value. Assignment binds; later reads look the value up. In Python the name has no fixed type, so the same name can hold different kinds of value over time.', paths: ['python', 'javascript'], example: { code: "pages = 12\npages = pages + 3     # rebind the name to a new value\nname, age = \"Hild\", 66   # two at once", language: 'python' } },
  { term: 'Virtual environment', definition: 'A private, per-project Python installation so different projects can use different package versions without conflict. Created with python -m venv .venv and activated in the shell.', paths: ['python'], aliases: ['venv', 'pip'], example: { code: "python3 -m venv .venv\nsource .venv/bin/activate    # Windows: .venv\\Scripts\\activate\npip install requests\npip freeze > requirements.txt", language: 'bash' } },
  { term: 'WebAssembly', definition: 'A binary instruction format browsers can run at near-native speed. It is how this site runs a full Python interpreter and SQLite inside your browser tab.', paths: ['internet', 'compilers'], aliases: ['wasm'] },
  { term: 'Window function', definition: 'A SQL function that computes over a set of related rows — a running total, a rank within a group — while keeping every row in the output. Written with OVER (PARTITION BY ... ORDER BY ...).', paths: ['sql'], match: ['window functions', 'OVER ('], example: { code: "SELECT name, department, salary,\n       RANK() OVER (PARTITION BY department ORDER BY salary DESC) AS dept_rank\nFROM employees;", language: 'sql' } },
  { term: 'with statement', definition: 'Runs a block with a resource that is set up before and cleaned up after, no matter what — files are closed, locks released. The resource is a context manager.', paths: ['python'], aliases: ['context manager'], match: ['with block', 'with statement'], example: { code: "with open(\"notes.txt\") as f:\n    text = f.read()\n# f is closed here, even if read() raised", language: 'python' } },
  { term: 'yield', definition: 'Turns a function into a generator: each yield hands out one value and pauses, resuming from the same spot on the next request.', paths: ['python'], example: { code: "def evens():\n    n = 0\n    while True:\n        yield n\n        n += 2\n\ng = evens()\nprint(next(g), next(g), next(g))", language: 'python' } },
]
